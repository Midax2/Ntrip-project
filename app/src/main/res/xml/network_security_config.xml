<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!-- Default: disallow cleartext traffic for all domains -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext traffic ONLY for specific NTRIP hosts that require HTTP.
         This limits the attack surface to just these endpoints. -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- EUREF NTRIP caster (example: euref-ip.net) -->
        <domain includeSubdomains="false">euref-ip.net</domain>
    </domain-config>

</network-security-config>
